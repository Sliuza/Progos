Este exemplo mostra como utilizar leds, usando um contador
para passar o tempo.

Para executar, compile

lasm -o test.exe test.s

e inicie o simulador

legsim -l test.exe -d devices.txt

No simulador, execute "g _start".
